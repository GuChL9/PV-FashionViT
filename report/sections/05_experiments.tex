\section{实验设计与探索过程}

\subsection{六组主实验}

表\ref{tab:experiment-matrix}按建立基线、扩大训练分布、改变聚合方式和加入局部先验的顺序组织实验。E1--E3 建立中心训练基线；E4 检查扩大训练分布的效果；E5 改变 token 聚合方式；E6 组合局部卷积特征与 Transformer。同一随机种子下，所有模型使用相同的数据划分、优化器和评估协议。

\begin{table}[H]
  \centering
  \caption{主实验矩阵及各实验回答的问题}
  \label{tab:experiment-matrix}
  \resizebox{\textwidth}{!}{%
    \begin{tabular}{lllll}
      \toprule
      编号 & 模型 & 训练分布 & 关键设置 & 主要问题 \\
      \midrule
      E1 & MLP & Center & 展平输入 & 缺少空间结构时位置退化有多明显？ \\
      E2 & CNN & Center & 局部卷积与池化 & 权值共享能否自然抵抗大幅平移？ \\
      E3 & ViT-AbsPos & Center & Learnable Pos + CLS & 全局交互是否仍会学习中心偏置？ \\
      E4 & ViT-Aug & Shift+Rotation & CLS + Random Erasing & 目标扰动增强组合能改善多少？ \\
      E5 & ViT-MeanPool & Shift+Rotation & Mean Pooling & 聚合方式是否改善稳定性？ \\
      E6 & HybridConv-ViT & Shift+Rotation & Conv Stem + Mean & 局部归纳偏置能否继续提升？ \\
      \bottomrule
    \end{tabular}%
  }
\end{table}

\subsection{三层对照逻辑}

六组实验可以合并为三层对照。第一层是 E1--E3：训练分布固定为 Center，只比较 MLP、CNN 与 ViT 的空间建模方式，用来判断中心测试是否掩盖位置失效。第二层是 E3--E4：ViT 结构保持不变，训练输入扩展为平移、旋转和擦除，观察分布覆盖带来的整体变化。第三层是 E4--E6：在相同增强方向上比较 CLS、Mean Pooling 以及卷积 stem 与 Transformer 的组合，考察聚合方式和局部视觉先验。按题目中的 A/B 记法，中心训练模型的 Center 与扰动测试分别对应 B/B 和 B/A，增强模型的 Center 与扰动测试则对应 A/B 和 A/A；项目用动态取样切换分布，不需要复制四套数据文件。

\input{tables/ab_protocol_matrix}

表\ref{tab:ab-protocol-matrix}把结构相同的 ViT-AbsPos 与 ViT-Aug 排成题目建议的四种设置。B/B 与 B/A 的落差说明中心训练的高分不能迁移到位置变化测试；A/B 与 A/A 的接近则说明扰动训练削弱了对测试位置的依赖。训练 A 同时包含旋转和随机擦除，各组件的贡献由后续消融实验进一步比较。

本轮选择 7 像素 patch，使 $56\times56$ 输入恰好形成 $8\times8$ 个 token，并把计算量控制在 CPU 可接受范围。由于没有训练其他 patch 配置，本文只说明这一取值，不讨论 patch 大小的优劣。

\subsection{逐项增强消融}

为进一步拆分 E3--E4，补充 Center、仅平移、平移+旋转、平移+旋转+Random Erasing 四个阶段。四者使用相同的 Tiny ViT、CLS pooling、优化器、数据划分和最多 12 epochs 的预算，只逐步扩大训练输入分布。

四个阶段都在随机种子 42、2026、3407 下独立训练，表格报告三次运行的均值与样本标准差。随机种子数量少于六组主实验，因此总体模型比较仍以五种子结果为准。

\subsection{位置编码消融}

为了直接回答位置编码本身的影响，项目另设 Learnable Absolute、固定二维 Sin-Cos 和 No Positional Encoding 三组。三者固定 Center 训练、CLS pooling、7 像素 patch、64 维 embedding、2 层 Encoder、优化器与最多 12 epochs，只切换位置编码，并同样使用随机种子 42、2026、3407。这样可以避免原可选 \texttt{vit\_sincos.yaml} 同时改变训练增强与 Mean Pooling 所带来的混杂。

No Pos 并不等于模型没有任何结构信息：卷积式 patch embedding 仍提取每个局部块，CLS token 也仍通过自注意力聚合所有 token；真正被移除的是把 token 映射到固定画布坐标的显式向量。因此该对照主要检查绝对坐标信息是否提高中心拟合、以及它是否加剧位置迁移下降。

这些实验不是完整的全因子设计。E6 建立在增强和 Mean Pooling 之上，因此卷积 stem 的比较仅对应当前增强与 pooling 设置。固定位置和固定角度扫描用于观察边缘位置和极端角度的表现。

\subsection{评价安排}

随机协议用于概括总体变化，49 点位置网格和固定角度扫描用于定位空间与方向上的薄弱区域，等半径裁剪对照进一步控制离中心距离，逐类结果、错误样本和 Attention Rollout 用于分析类别差异。六组主实验汇总五个随机种子，两条控制变量实验汇总三个随机种子；Attention Rollout 仅作定性分析。
